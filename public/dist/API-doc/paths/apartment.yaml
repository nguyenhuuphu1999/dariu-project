get:
  tags:
  - Users
  summary: /apartment
  consumes:
  - "application/json"
  produces:
  - "application/json"
  parameters:
  - in: body
    name: data
    description: jhhhj
  schema:
    type: object
    properties:
      name:
        type: string
        example: User 1
      password:
        type: string
        example: "khk"
  responses:
    200:
      description: Create user success
      schema:
        type: object
        properties:
          id:
            type: int (auto)
            example: 1
          image_url:
            type: string
            example: home/home_doc_dao
          title:
            type: string
            example: Chỗ ở độc đáo
          so_luong_can_nha:
            type: string
            example: 30

        
