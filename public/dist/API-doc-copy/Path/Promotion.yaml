get:
  tags:
  - Promotion
  summary: 
  consumes:
  - "application/json"
  produces:
  - "application/json"
  parameters:
  - in: body
    name: data
    description: page Promotion
  schema:
    type: object
    properties:
      name:
        type: string
        example: User 1
      password:
        type: string
        example: "khk"
  responses:
    200:
      description: Page promotion 
      schema:
        type: object
        properties:
          
              